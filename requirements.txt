# Rising Sun Events — Sales Rep Chatbot
# Python dependencies
# Install with: pip install -r requirements.txt

# ── Core web framework ───────────────────────────────────────
streamlit>=1.35.0

# ── AI / LLM ─────────────────────────────────────────────────
anthropic>=0.28.0

# ── Vector database + embeddings ─────────────────────────────
chromadb>=0.5.0
sentence-transformers>=3.0.0   # Embedding model (downloads ~80 MB on first run)

# ── Document parsing ─────────────────────────────────────────
pypdf>=4.2.0                   # PDF reading
python-docx>=1.1.0             # Word document reading

# ── Utilities (usually already installed, listed for clarity) ─
# json, csv, hashlib, os, pathlib — all part of Python stdlib
